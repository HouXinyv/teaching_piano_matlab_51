#include <reg51.h>
#include "anjian.h"
#include "data.h"


//数据定义：
//音符码格式: | D7 | D6 | D5 D4 | D3 | D2 D1 D0 |
// |升降 |空闲 | 八度 |空闲 | 音符 |
//音符长度： | D7 D6 | D5 | D4 | D3 | D2 | D1 | D0 |
// |空闲 |空闲 | 全 |二分 |四分|八分|16 分|32 分|


//函数定义:

void main(void)
{
    

		repeat=10000/SPEED;
    TMOD=0x11; //定时器模式：定时器 0 和定时器 1 均工作在方式 1，16 位定时器
    EA=1; //开总中断
    ET0=1; //开定时器 0 中断
    ET1=1; //开定时器 1 中断
    TR0=0; //定时器 0 停止计数
    TR1=0; //定时器 1 停止计数
    music_play();
    while(1);// {

			  
   // }
}
